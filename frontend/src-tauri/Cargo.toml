[package]
name = "fom-oracle"
version = "0.1.0"
edition = "2021"
description = "Shell desktop Tauri do FOM Oracle"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
